node {
    stage('Declarations') {
        echo "Running on branch ${env.BRANCH_NAME}"
    }
    stage('Build') {
        echo 'Installing npm modules...'
    }
    stage('Deploy') {
        echo 'Deploying... nowhere.'
    }
}
